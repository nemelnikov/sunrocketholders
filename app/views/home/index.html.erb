<% provide(:title, "Регайся по данной ссылке и никому не будет обидно") %>
<table class="table table-bordered">
  <tbody>
  <tr>
    <th class="col-sm-2">Кому поможем</th>
    <td><%= @link.name %></td>
  </tr>
  <% if !@link.rocket.empty? %>
  <tr>
    <th class="col-sm-2">Ссылка на Рокет</th>
    <td> <%= link_to "Заказать Рокет", @link.rocket %>
  </tr>
  <% end %>
  <% if !@link.tinkoff.empty? %>
  <tr>
    <th class="col-sm-2">Ссылка на Тиньков</th>
    <td> <%= link_to "Тинькова тоже можно заказать", @link.tinkoff %>
  </tr>
  <% end %>

  <tr>
    <td colspan="2"> <%= link_to "Написать в телеграм и порадовать, что заказали карту", @link.telegram %>
  </tr>

  <tr>
    <td colspan="2"> Хочешь добавить свою карту сюда? Напиши Никите:)
  </tr>
  </tbody>
</table>
